<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reminderapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReminderApp">
        <activity android:name=".Snooze">
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:excludeFromRecents="true"
        </activity>
        <activity
            android:label="Remind Me!"
            android:name=".AddAlarm" >

            <!-- intent-filter section used for Chapter 10 & Chapter 11 Act #1
                 and will be commented out in Chapter 11 Activity #2 -->

            <!-- <intent-filter >
            <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> -->
        </activity>

        <activity
            android:label="Remind Me!"
            android:name=".AlarmList" >

            <!-- Student will add intent-filter sectn here in Chap 11 Act #2 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>